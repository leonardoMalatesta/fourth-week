import { UnslessDirective } from './unsless.directive';

describe('UnslessDirective', () => {
  it('should create an instance', () => {
    const directive = new UnslessDirective();
    expect(directive).toBeTruthy();
  });
});
